{
  "name": "webhook-task-pipeline",
  "version": "1.0.0",
  "description": "A webhook ingestion service that queues jobs for background processing and delivers processed results to registered subscribers",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "worker": "ts-node-dev --respawn --transpile-only src/workers/jobWorker.ts",
    "lint": "eslint .",
    "lint:frontend": "npm --prefix frontend run lint -- --max-warnings=0",
    "format:write": "prettier --write \"src/**/*.{js,ts,json,css,md}\"",
    "format:check": "prettier --check \"src/**/*.{js,ts,json,css,md}\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "pg": "^8.19.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.3",
    "@types/pg": "^8.18.0",
    "eslint": "^10.0.2",
    "globals": "^17.4.0",
    "prettier": "3.8.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.0"
  }
}
